[project]
name = "fauxspark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.5",
    "simpy>=4.1.1",
]

[tool.ruff]
line-length = 100